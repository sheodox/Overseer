/*
 x client gets session cookie
 x client types name
 x send name proposal to server
 server validates
 server adds to _users state
 server sends name

 */

const actions = {

};

export default actions;