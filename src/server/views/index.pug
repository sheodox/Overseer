extends layout
block content
    div#app-root
    script.
        window.user = !{user};
        window.serverMetadata = !{serverMetadata};
        window.Booker = !{permissions};
    script(src='/' + manifest['main.js'])
